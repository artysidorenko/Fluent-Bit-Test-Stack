[SERVICE]
    Flush        1
    Daemon       Off
    Log_Level    info
    Parsers_File parsers.conf
    HTTP_Server  On
    HTTP_Listen  0.0.0.0
    HTTP_Port    2020

    # # Uncomment this section to save flb chunks to disk for additional debugging
    # storage.path              /var/log/flb-storage/flb-storage
    # storage.sync              normal
    # storage.checksum          off
    # storage.backlog.mem_limit 5M
